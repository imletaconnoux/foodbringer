<%= form_for @item do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.label :price %>
	<%= f.text_field :price %>
	<%= f.hidden_field :chef_id, value: current_user.id %>
	<%= f.submit 'Save' %>
<% end %>